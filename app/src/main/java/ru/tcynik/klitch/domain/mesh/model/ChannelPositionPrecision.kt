package ru.tcynik.klitch.domain.mesh.model

/** Meshtastic channel `module_settings.position_precision` (0 = geo toggles off in channel settings). */
object ChannelPositionPrecision {
    const val DISABLED = 0
    const val ENABLED = 32
}
